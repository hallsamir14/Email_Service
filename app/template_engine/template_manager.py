"""
User will need to create their own email templates to be mapped with the corresponding defined message type(s). 
User will provide markdown files to be managed by template manager 
Template manager will ensure markdown files provided by user are properly processed upon reception and managed according to app behavior

Primary function:
Ensure files are markdown format
Ensure filenames are correct
what else?
"""
